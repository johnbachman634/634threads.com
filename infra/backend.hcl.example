# Copy to backend.hcl (gitignored) and fill in, then:
#   terraform init -backend-config=backend.hcl
bucket       = "your-terraform-state-bucket"
key          = "634threads.com/terraform.tfstate"
region       = "us-west-2"
use_lockfile = true
